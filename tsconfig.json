{
  "compilerOptions": {
    "target": "es2016", // 컴파일 타겟 es 버전
    "module": "commonjs", // 모듈 시스템 종류
    "lib": ["dom", "dom.iterable", "esnext"], // 컴파일 과정에서 사용될 라이브러리 파일 목록
    "strict": true, // 엄격모드
    "allowJs": true, // js  파일을 ts 파일에 import하는 것을 허용
    "jsx": "preserve", // JSX 코드를 어떻게 컴파일할지 설정
    "isolatedModules": true, // Babel이 TypeScript 프로젝트의 파일을 안전하게 트랜스파일할 수 있는지 확인합니다.
    "moduleResolution": "node", // 모듈 해석 방법 설정
    "resolveJsonModule": true, // 확장자가 .json인 모듈의 import 허용 여부
    "esModuleInterop": true // CommonJS와 ES Modules 간의 상호 운용성이 생기게 할 지 여부
  },
  "exclude": ["node_modules"], // node_modules은 컴파일 금지
  "include": ["**/*.ts", "**/*.tsx"] // 컴파일 할 파일
}
